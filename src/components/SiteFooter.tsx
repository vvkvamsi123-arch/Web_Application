export function SiteFooter() {
  return (
    <footer className="mt-16 border-t border-amber-200/80 bg-white/80">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-2 px-4 py-6 text-sm text-slate-600 sm:px-6 md:flex-row md:items-center md:justify-between">
        <p>Phase 1 static storefront for HarborCart.</p>
        <p>Designed to migrate to microservices in later phases.</p>
      </div>
    </footer>
  );
}
